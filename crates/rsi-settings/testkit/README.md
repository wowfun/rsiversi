# rsi-settings-testkit

This package provides a deterministic in-memory Settings provider ordinary
plugin. It implements the same raw-section CAS seam as local persistence and
performs no filesystem, environment, credential, or network access.
