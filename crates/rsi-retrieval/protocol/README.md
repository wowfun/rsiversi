# rsi-retrieval-protocol

Finite, runtime-independent configuration, closed errors and durable external
source DTOs for the [retrieval contract](../README.md). Historical consumers
validate these values before presentation. Syntactic URL validation here is not
network admission: the core owner additionally checks all resolved addresses.
