# rsi-permission-presets

This package owns the frozen permission-preset service contract and ordinary
plugin. Configuration is a complete exact-name map. Each preset selects one
Sandbox mode and whether interactive approval is required.
