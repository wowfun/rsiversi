# rsi-credentials-testkit

This package provides an ordinary in-memory Credentials plugin for deterministic
tests. It uses exact owner/slot addresses and the same redacted Resolve/Admin
contracts without reading a real keyring or ambient environment.
