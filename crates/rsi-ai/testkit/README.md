# rsi-ai-testkit

This package supplies deterministic scripted Language and Image adapters, a
request-aware language adapter, and a digest-addressed in-memory media
resolver. It is test infrastructure, not a production provider.
