# rsi-ai-testkit

This package supplies deterministic scripted adapters for all five capabilities,
a request-aware language adapter, an interactive Realtime script, and a
digest-addressed in-memory media resolver. It is test infrastructure, not a
production provider.
