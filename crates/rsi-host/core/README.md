# rsi-host

This package implements the generic static HostBuilder, resolved Profile
bootstrap, replay convergence, observation, and shutdown API defined by the
family [contract](../README.md). It owns no standard product implementation.
