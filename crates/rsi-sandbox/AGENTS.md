Read the family [contract](README.md) before changing sandbox behavior.

- Record only enforcement actually selected by the provider. Prompt text,
  requested mode, and feature presence are not enforcement stamps.
- Restricted modes fail closed when no verified backend is available.
- Default tests script probes and inspect plans; they do not mutate host policy
  or claim native Windows/macOS coverage.
