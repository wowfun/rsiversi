Read the family [contract](README.md) before changing ACP wire or ownership behavior.

- Validate external frames and method parameters before permissive schema decoding.
  Error diagnostics must not echo peer payloads or launch credentials.
- Keep process lifecycle in Process and native Session state in the Agent owner.
  Protocol handlers must not acquire ambient execution or filesystem authority.
- Test malformed input, retention admission, cancellation and writer drain through
  public byte transports. Label fixture protocol evidence separately from native
  Session, independent SDK and live-provider interoperability.
