# rsi-agent security

The active protocol validates and bounds tool envelopes but does not execute tools or provide a sandbox. A future runtime must define its own durable-state and execution trust boundaries before it becomes an active product.
