# rsi-lsp

The [stdio plugin](core/README.md) is an optional, independently composable source
addon for four read-only language queries. It consumes public Process, Sandbox,
Files and Tools contracts; Kernel and Meta contain no language-server policy.
The standard product owns the [UI adapter](../rsi/lsp-ui/README.md).
